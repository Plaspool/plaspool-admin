/**
 * Drizzle table definitions.
 *
 * Placeholder until Task 2. `server/db/client.ts` and `server/test/harness.ts`
 * both `import * as schema from './schema'`, so this module has to exist for
 * Task 1's `tsc -b` gate to pass — Task 2 fills it in.
 */
export {};
