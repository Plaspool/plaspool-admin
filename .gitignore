# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real credentials. `.env.example` is the checked-in template.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Personal Claude Code overrides for this checkout. `.claude/settings.json`
# would be the shared, committed one; this is not it. (`*.local` above does not
# cover it — that matches names ending in `.local`, not `settings.local.json`.)
.claude/settings.local.json

# Server bundle for the Vercel function. Build output, like dist/.
api-build
